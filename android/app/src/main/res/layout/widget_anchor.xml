<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Colorable pill background. Tinted at runtime via
         ImageView.setColorFilter(int), which preserves the rounded
         corners — plain setBackgroundColor() on a shape drawable would
         replace it with a flat ColorDrawable and lose the shape. -->
    <ImageView
        android:id="@+id/widget_pill_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/widget_pill_shape"
        android:contentDescription="@null" />

    <LinearLayout
        android:id="@+id/widget_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="22dp"
        android:paddingEnd="8dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp">

        <TextView
            android:id="@+id/widget_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Vayimaen"
            android:textSize="17sp"
            android:textStyle="bold"
            android:textColor="#2B2B2B"
            android:singleLine="true"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_cycle_btn"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginStart="8dp"
            android:gravity="center"
            android:text="&#8635;"
            android:textSize="20sp"
            android:textColor="#5A5A5A"
            android:background="@drawable/widget_cycle_circle_bg"
            android:contentDescription="Switch widget action" />

    </LinearLayout>
</FrameLayout>
